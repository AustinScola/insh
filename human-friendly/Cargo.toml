[package]
name = "human-friendly"
version = "0.1.0"
edition = "2021"
description = "Renders information about files the way that people are used to reading it."

[dependencies]
# Used for formatting the times that files were modified in the local time zone.
chrono = { workspace = true }

[dev-dependencies]
test-case = { workspace = true }
