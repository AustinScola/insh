/// A horizontal alignment.
#[derive(Clone, Copy)]
pub enum Align {
    /// A left horizontal alignment.
    Left,

    /// A center horizontal alignment.
    Center,

    /// A right horizontal alignment.
    #[allow(dead_code)]
    Right,
}
