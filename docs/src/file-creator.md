# File Creator

The file creator can be used to create a file in a directory.

The file creator shows the directory at the top, then an input bar, then any error from attempting to
create a new file.
