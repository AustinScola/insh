[package]
name = "config"
version = "0.1.0"
edition = "2021"

[dependencies]
dirs = "4.0.0"

# Used for serialization and deserialization of data structures.
serde = { version = "1.0.144", features = ["derive"] }
serde_yaml = "0.9.10"

[lints]
workspace = true
